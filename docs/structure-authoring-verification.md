# Structure Authoring 升级验收 — 2026-09-08

## 实施结果
- 独立 authoring-sdk：房间、入口、灯具、组件的几何与语义同步；精确变换与冻结侧车。
- 不可变源码项目、目标依赖、文件级更新和修订检查；兼容 inline DrawProgram。
- 编译缓存与有界快照解析缓存；不同请求仍执行绘图，不假设任意 Java 是纯函数。
- 分阶段预检、真实方块字典、空间诊断、固定取景、多视图、组件筛选、照明/通路叠加。
- 草稿与计划原子提交、持久化失败保护、显式归档/恢复、阶段耗时统计。
- MCP 服务拆分、精简契约入口及章节读取；旧完整契约、格式 1/2 发布包与旧结构读取继续兼容。

## 验收
- 主程序、客户端、Mod JAR 编译通过。
- **82 项定向测试通过，0 失败、0 跳过**，包括文档中两套完整 MCP 示例回放。
- Java 25/26 的无系统编译器 worker 回归通过。
- 源码依赖失效、热缓存、损坏缓存重建、归档/恢复、持久化失败注入、组件变换通过。
- 像素级验证确认 frame 和 overlays 改变真实 PNG；模型诊断图已检查。
- 实际 Minecraft 26.2 / Fabric 隔离新世界验证了 authored 输出：群组覆盖 24 个区块，独立结构覆盖 15 个区块。
- 床、门、双格植物配对完整；正常保存、重载及同种子第二个新世界的全部检查区域方块状态哈希完全相同。
- 加载原生数据包没有创建源码执行目录。测试服务器均正常停止。

## 性能实测边界
本次受控样例首次 hall 编译 727 ms；同源码参数变体从缓存恢复 6 ms，并继续执行绘图。
完整数据包含正常冷/热构建、公共依赖变更和主动损坏缓存恢复，不把整个混合序列当作纯缓存命中率基准。
这只是宿主阶段耗时，不是模型思考耗时，也不代表完整 AI 创作流程的提速倍数。

## 交付物
- Mod：`build/authoring-delivery/worldsmith-0.1.0.jar`，5693900 bytes。
- SHA-256：`5a81e250daa85f6a94c69de5dc879c9ed57436da2253b7befd57b7456e6b9a3e`。
- 机器可读验收：`build/authoring-delivery/verification.json`。
- 诊断图：`build/authoring-delivery/diagnostic-preview.png`。
- 文档与源码示例：[工作台说明](structure-authoring-workbench.md)。

## 未扩展的边界
不运行全量测试、游戏 GUI 自动化或安装器打包；没有编辑玩家已有存档，也没有提交或推送 Git。
原生预检涵盖状态/配对/发光；完整内容与数据包激活仍走最终发布检查。预览不等于实际光照或审美评分。
仍保持原有生成、采样、写入预算及 landmarkInstancesVerified=false。
构建使用 build/structure-efficiency-validation 中的独立缓存，避免干扰正在使用的开发客户端。
按照最后一条用户要求保持电脑开机；没有安排或执行关机。
